# similar to list
# we cannot add or remove items to them,
# they are immutable
# lists are defined by square brackets, tuples are defined by normal bracket
# useful when you want to have a static list that you want to be sure wont be changed in the program

numbers = (1, 2, 3, 4, 5)
print(numbers[1])
